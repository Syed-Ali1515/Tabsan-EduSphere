@model Tabsan.EduSphere.Web.Models.Portal.CoursesPageModel
@{ ViewData["Title"] = "Courses & Offerings"; }

@if (!string.IsNullOrWhiteSpace(Model.Message))
{
    <div class="alert alert-danger">@Model.Message</div>
}

@if (!Model.IsConnected)
{
    <div class="alert alert-warning">Not connected. <a asp-action="Dashboard">Configure API</a>.</div>
}
else
{
    <div class="card border-0 shadow-sm mb-3">
        <div class="card-body">
            <form method="get" asp-action="Courses" class="row g-2 align-items-end">
                <div class="col-md-4">
                    <label class="form-label">Filter by Department</label>
                    <select name="departmentId" class="form-select" onchange="this.form.submit()">
                        <option value="">All Departments</option>
                        @foreach (var d in Model.Departments)
                        {
                            <option value="@d.Id" selected="@(Model.SelectedDepartmentId == d.Id ? "selected" : null)">@d.Name</option>
                        }
                    </select>
                </div>
            </form>
        </div>
    </div>

    <div class="row g-3">
        <div class="col-12 col-xl-6">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h5 mb-3">Courses <span class="badge bg-secondary ms-1">@Model.Courses.Count</span></h2>
                    @if (Model.Courses.Count == 0)
                    {
                        <p class="text-muted">No courses found.</p>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-sm table-hover mb-0">
                                <thead class="table-light">
                                    <tr><th>Code</th><th>Title</th><th>Dept</th><th>Credits</th></tr>
                                </thead>
                                <tbody>
                                    @foreach (var c in Model.Courses)
                                    {
                                        <tr>
                                            <td><code>@c.Code</code></td>
                                            <td>@c.Title</td>
                                            <td>@c.DepartmentName</td>
                                            <td class="text-center">@c.CreditHours</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>
        </div>
        <div class="col-12 col-xl-6">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h5 mb-3">Active Offerings <span class="badge bg-secondary ms-1">@Model.Offerings.Count</span></h2>
                    @if (Model.Offerings.Count == 0)
                    {
                        <p class="text-muted">No offerings found.</p>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-sm table-hover mb-0">
                                <thead class="table-light">
                                    <tr><th>Course</th><th>Faculty</th><th>Semester</th><th>Active</th></tr>
                                </thead>
                                <tbody>
                                    @foreach (var o in Model.Offerings)
                                    {
                                        <tr>
                                            <td>@o.CourseCode — @o.CourseTitle</td>
                                            <td>@o.FacultyName</td>
                                            <td>@o.SemesterName</td>
                                            <td class="text-center">
                                                <span class="badge @(o.IsActive ? "bg-success" : "bg-secondary")">
                                                    @(o.IsActive ? "Yes" : "No")
                                                </span>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
}
